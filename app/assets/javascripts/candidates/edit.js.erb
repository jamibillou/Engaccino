$(document).ready(function()
{
  // CUSTOMS THE FORMS
  customForm("candidate_signup_form","candidates.");
  customForm("candidate_edit_form","candidates.");
  
  $("#country").autocomplete(
  {
  	source:"/ajax/countries",
    autoFocus: true,
    minLength: 2
  });
})