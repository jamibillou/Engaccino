$(document).ready(function()
{
  // CUSTOMS THE FORMS
  customForm("user_signup_form","users.");
  customForm("user_edit_form","users.");
  
  $("#country").autocomplete(
  {
  	source:"/ajax/countries",
    autoFocus: true,
    minLength: 2
  });
})