%ul
  %li
    = link_to t('all'), path, :class => 'black', :id => 'all', :remote => true
  %li
    = link_to t('users.following'), "#{model}/#{current_user.id}/following", :class => 'black', :id => 'following' , :remote => true
  %li
    = link_to t('users.followers'), "#{model}/#{current_user.id}/followers", :class => 'black', :id => 'followers', :remote => true
