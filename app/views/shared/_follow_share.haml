#follow_share
  
  #follow_form
    - if current_user.following? user
      = render 'shared/unfollow', :user_id => user.id
    - else
      = render 'shared/follow', :user_id => user.id
      
  = link_to t('share'), '#', :class => 'round grey button'