<div id="education" class="white_block">
  <h2><%=t "candidates.education.title" %></h2>
  <%= f.fields_for :educations %>
  <%= f.link_to_add '+', :educations, :class => 'button blue round', :title => t('candidates.education.add') %>
</div>