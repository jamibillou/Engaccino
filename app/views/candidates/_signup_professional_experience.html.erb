<div id="professional_experience" class="white_block">
  <h2 class="no_margin_bottom"><%=t "candidates.professional_experience" %></h2>
  <%= fields_for :experiences do |experience_builder| %>
    <%= render 'experience_fields', :f => experience_builder %>
  <% end %>
  <%= link_to_add_fields('+', f, :experiences, 'slightly_round blue button') %>
</div>