<% unless @contacts.nil? %>
  <div id='menu_left'>  
    <%= render :partial => 'menu_left', :locals => { :contacts => @contacts, :messages => @messages } %>
  </div>
  <div id='conversation'>
    <%= render 'conversation', :contact => @contacts.first %>
  </div>
<% else %>
  <%=t 'messages.no_message' %>
<% end %>
