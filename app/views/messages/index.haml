- unless @contacts.nil?
  #menu_left
    = render :partial => 'menu_left', :locals => { :contacts => @contacts, :messages => @messages, :current_contact => @current_contact }
  #conversation
    = render 'conversation', :contact => @contacts.first
- else
  = t('messages.no_message')
