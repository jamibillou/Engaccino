<%= form_for :interpersonal_skill_candidate, :remote => true do |f| %>
  <%= f.text_field :label, :class => 'edit_field margin' %><!-- à remplacer par un fields for :interpersonal_skill une fois le controller en place -->
  <%= f.text_area :description, :class => 'textarea_edit' %>
<% end %>